# TungsAcu-DB Workspace Rules

正式開發與唯一可寫入的 repo：

```text
/Users/samue11in/Projects/TungsAcu-DB
```

SynologyDrive 內同名 repo 是歷史／參考副本，不得新增或修改程式、CSV、spec、plan 或工作日誌。書籍來源 Markdown 可從雲端 `AI_Projects/04-書籍資料庫/` 讀取。

每次寫檔前必須：

1. 執行 `pwd` 與 `git rev-parse --show-toplevel`。
2. 確認 Git root 完全等於 `/Users/samue11in/Projects/TungsAcu-DB`。
3. 先說明將寫入的絕對路徑。
4. 若 Git root 位於 SynologyDrive，停止並切換到本機正式 repo。

TungsAcu-DB 專屬 spec 固定放在本 repo 的 `docs/specs/`。系統級工具或跨專案 spec 才放雲端 `99-工具與系統/`。
